import { expect } from 'chai';

import getLanguageReport from '~/src/getLanguageReport';

describe('getLanguageReport', () => {
  it('should give back the full report for a language', () => {
    const actual = {};
    const expected = {};
    expect(actual).to.deep.equal(expected);
  });
});
